import fetchSupplierStatus from "@/apollo/query/fetchSupplierStatus.gql";
export default function useSupplierStatus(uid: string) {
  // const { onResult, onError } = useCustomQuery(fetchSupplierStatus, { uid });
  // onResult((data) => {
  //   console.log(data);
  //   return { data: "data" };
  // });
  // onError((error) => {
  //   console.log(error);
  //   return { error: "error" };
  // });
}
